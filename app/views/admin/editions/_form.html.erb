<%= form_for([:admin, @edition], :html => { :class => "form-default"}) do |f| %>
  <% if @edition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@edition.errors.count, "error") %> prohibited this edition from being saved:</h2>

      <ul>
      <% @edition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>

      <label class="control-label" for="inputLabel"><%= f.label :label %></label>
      <%= f.text_field :label %>
      
      <div>
        <%= f.submit 'Save', :class => 'btn' %>
      </div>
  </fieldset>
<% end %>
