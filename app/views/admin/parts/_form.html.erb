<%= form_for([:admin, @part], :html => { :class => "form-default"}) do |f| %>
  <% if @part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@part.errors.count, "error") %> prohibited this part from being saved:</h2>

      <ul>
      <% @part.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>

      <label class="control-label" for="inputEmail"><%= f.label :sorting %></label>
      <%= f.text_field :sorting %>


      <label class="control-label" for="inputEmail"><%= f.label :label %></label>
      <%= f.text_field :label %>
      
      <div>
        <%= f.submit 'Save', :class => 'btn' %>
      </div>
  </fieldset>
<% end %>
